# Development Branches

`baseline/v2` is the preserved V2 reference implementation.

Feature work branches from that baseline and should merge into `dev/format-aware-engine` through pull requests. Feature branches should not rewrite `baseline/v2`.

Current feature branches:

- `dev/harr-parser`
- `dev/jpeg-mpo-recovery`
- `dev/raw-formats`
